namespace Interchée.Entities.Enums
{
    public enum SubmissionStatus
    {
        NotStarted = 1, // No submission yet
        Submitted = 2,  // Student submitted work
        Reviewed = 3    // Graded by supervisor
    }
}
